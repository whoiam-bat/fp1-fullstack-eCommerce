import { CheckoutFormValidator } from './checkout-form-validator';

describe('CheckoutFormValidator', () => {
  it('should create an instance', () => {
    expect(new CheckoutFormValidator()).toBeTruthy();
  });
});
